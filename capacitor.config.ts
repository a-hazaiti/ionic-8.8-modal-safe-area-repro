import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-8.8-modal-safe-area-repro',
  webDir: 'www'
};

export default config;
